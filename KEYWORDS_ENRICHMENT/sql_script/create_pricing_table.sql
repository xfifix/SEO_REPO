CREATE TABLE IF NOT EXISTS PRICING_KEYWORDS (
    KEYWORD VARCHAR(300) NOT NULL, 
    LANGUAGE VARCHAR(2),
    SEARCH_ENGINE VARCHAR(2),
    CHECK_DATE DATE,
    URL TEXT,
    SEARCH_POSITION INT,
    ABSOLUTE_POSITION INT,
    PAGE_NUMBER INT,
    RESULT_NUMBER BIGINT,
    STAR_NUMBER INT,
    CPC_MAX REAL,
    SEARCH_VOLUME INT,
    COMPETITION INT, 
    PARAMETER VARCHAR(12),
    DOMAIN VARCHAR(69),
    SUBDOMAIN VARCHAR(221),
    MAGASIN VARCHAR(200),
    RAYON VARCHAR(200),
    PRODUIT VARCHAR(400)
);